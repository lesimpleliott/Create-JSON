// ***** SETTINGS FILE *****

const outputFileExtension = "webp"; // Extension des fichiers de sortie
const outputSize = 0.5; // Taille de l'image de sortie (0.1 à 1)
const defaultAuthor = "unsplash"; // Auteur par défaut
const outputJSONFile = "exportData"; // Nom du fichier JSON de sortie

module.exports = {
  outputFileExtension,
  outputSize,
  defaultAuthor,
  outputJSONFile,
};
